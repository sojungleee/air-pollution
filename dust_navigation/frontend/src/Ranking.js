import React from 'react';
import styled from 'styled-components';

const Ranking = (props) => {
    return (
        <div>
            ranking page test
        </div>
    );
}

export default Ranking;